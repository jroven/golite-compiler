func add(a int, b int) int {
    return (a + b);
} 

func main() {

    var a, b int;
    a = 3;
    scan b;

    for ((b == 1) && (a < 8)) {
        printf("a = %d\n", a);
        a = add(a, 1);
    }

    printf("a = %d\n", a);
    a = 4;
}