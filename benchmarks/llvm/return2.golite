func foo(x int) int {
    if (x < 6) {
        return 0;
    }
    return 3;
}

func main() {
    var a, b int;
    a = foo(5);
    printf("a = %d\n", a);
    b = foo(10);
    printf("b = %d\n", b);
}